---
/**
 * Página de diagnóstico temporal (no indexada).
 * El contenido vive en `SafeAreaDiag.astro` porque ahora mismo también
 * se está sirviendo en la portada. Ambas se retiran en cuanto se cierre
 * el asunto de la barra en iOS.
 */
import SafeAreaDiag from '../components/SafeAreaDiag.astro';
---

<SafeAreaDiag />
